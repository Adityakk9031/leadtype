---
"leadtype": patch
---

Fix sparse array holes in search index chunk headingPath when markdown headings skip levels or start below h1. This also prevents createAskHandler from throwing leadtype: generated NLWeb artifacts have an invalid shape when indexed pages omit an h1 heading or skip levels.